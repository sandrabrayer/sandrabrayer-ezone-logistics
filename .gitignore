# Dependencies
node_modules/

# Environment & secrets — NEVER commit real values
.env
.env.local
.env.*.local
*.key
*.pem
credentials.json
client_secret*.json
service-account*.json

# Apps Script credentials (clasp) — NEVER commit. These hold OAuth tokens.
# (.clasp.json IS committed — it holds only the non-secret Script ID.)
.clasprc.json
.clasp.local.json

# Logs / OS / editor
*.log
npm-debug.log*
.DS_Store
.idea/
.vscode/
